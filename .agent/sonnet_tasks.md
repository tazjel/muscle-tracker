# GTD3D — Sonnet Task Waves

> Generated by: Opus (2026-04-10)
> Project: ~/Projects/gtd3d/
> Instructions: Read `.agent/sonnet_instructions.md` FIRST

---

## Wave 3A — APK Screen Extraction (5 tasks)

Goal: Extract 15 screen classes from `companion_app/lib/main.dart` (2,074 lines) into individual files under `companion_app/lib/screens/`. Target: main.dart drops to ~800 lines (only HomeScreen + imports + routes).

### Prerequisites
- Read `sonnet_instructions.md` for RTK and tool usage rules
- `Grep "^class" companion_app/lib/main.dart` to get CURRENT line numbers (do NOT trust numbers below)

### Tasks

- [x] **3A.1 — Extract simple screens (6 classes)**
  Extract these stateless/simple screens into `companion_app/lib/screens/`:
  - `ReviewScreen` → `review_screen.dart`
  - `ReportViewerScreen` → `report_viewer_screen.dart`
  - `ProfileProgressScreen` → `profile_progress_screen.dart`
  - `RegisterScreen` → `register_screen.dart`
  - `HealthLogScreen` + `HealthLogListScreen` → `health_log_screen.dart` (both in one file, they're related)
  
  **How**: Read the class + its State class. Write to new file with needed imports. Remove from main.dart.
  **Don't**: Add new imports, refactor logic, add comments, or change any behavior.
  **Verify**: `Grep "class ReviewScreen" companion_app/lib/screens/review_screen.dart` confirms extraction.

- [x] **3A.2 — Extract medium screens (4 classes)**
  - `LoginScreen` → `login_screen.dart`
  - `ResultsScreen` → `results_screen.dart`
  - `ProgressScreen` → `progress_screen.dart`
  - `HistoryScreen` → `history_screen.dart`
  
  **Same process**: Read class boundaries with Grep, extract, remove from main.dart.

- [x] **3A.3 — Extract complex screens (3 classes)**
  - `ProfileSetupScreen` → `profile_setup_screen.dart` (~270 lines, has form logic)
  - `LivePreviewScreen` → `live_preview_screen.dart` (~220 lines, has camera logic)
  - `ModelViewerScreen` → `model_viewer_screen.dart` (~55 lines)
  
  **Warning**: ProfileSetupScreen and LivePreviewScreen are large. Read their full class boundaries carefully before extracting.

- [x] **3A.4 — Extract BodyScanReview + RegionRecapture**
  - `BodyScanReviewScreen` + `_RegionRecaptureScreen` → `body_scan_review_screen.dart`
  - These two are tightly coupled (RegionRecapture is used inside BodyScanReview). Keep in same file.
  - Rename `_RegionRecaptureScreen` to `RegionRecaptureScreen` (remove private prefix since it's now in its own file, but keep it package-private by not exporting).

- [x] **3A.5 — Update imports + verify build**
  - In `main.dart`: add `import 'screens/xxx.dart';` for each extracted file
  - In `main.dart`: verify route table still references correct class names
  - Run: `cd C:/Users/MiEXCITE/Projects/gtd3d/companion_app && rtk flutter.bat build apk --debug`
  - If build fails: fix import errors. Common issue: missing imports in extracted files (Material, http, etc.)
  - **COMMIT**: `rtk git add -f companion_app/ && rtk git commit -m "feat(apk): extract 15 screens from main.dart (2074→~800 lines)"`

---

## Wave 3B — APK Services Implementation (3 tasks)

Goal: Fill the 3 stub service files (currently 6 lines each) with real implementations. Wire them into HomeScreen.

### Prerequisites
- Wave 3A must be committed first (services may import screen types)
- Grep `http.get\|http.post` in `companion_app/lib/` to find all HTTP patterns to centralize
- Read `companion_app/lib/config.dart` to understand AppConfig (serverBaseUrl, jwtToken)

### Tasks

- [x] **3B.1 — Implement api_service.dart**
  Current: 6-line stub. Target: ~150-200 lines.
  - Singleton pattern: `static final ApiService instance = ApiService._();`
  - Base URL from `AppConfig.serverBaseUrl`, JWT from `AppConfig.jwtToken`
  - Methods to implement (grep existing tabs/ for HTTP calls to find the patterns):
    - `Future<Map> get(String path)` — GET with auth header
    - `Future<Map> post(String path, {Map? body, File? file})` — POST with auth, multipart for files
    - `Future<Map> uploadImage(String path, File image)` — multipart upload
  - Extract repeated `http.get(Uri.parse('${AppConfig.serverBaseUrl}/...')` patterns
  - **Don't**: Add retry logic, caching, interceptors, or anything not already in the codebase.

- [x] **3B.2 — Implement camera_service.dart + sensor_service.dart**
  **camera_service.dart** (~80-100 lines):
  - Singleton owning `CameraController`
  - `Future<void> initialize(ResolutionPreset preset)` — init camera
  - `Future<XFile> capture()` — take picture
  - `void toggleTorch()` — toggle flash
  - `void dispose()` — cleanup
  - Grep `CameraController` in main.dart and tabs/ to find current usage patterns. Mirror them.
  
  **sensor_service.dart** (~60-80 lines):
  - Singleton owning accelerometer/gyro/magnetometer streams
  - Expose `ValueNotifier<double> pitch` and `ValueNotifier<double> roll`
  - Grep `accelerometer\|gyroscope\|magnetometer` to find current subscription patterns

- [x] **3B.3 — Wire services into HomeScreen + wire build**
  - In `_HomeScreenState`: replace direct CameraController creation with `CameraService.instance`
  - Replace direct sensor subscriptions with `SensorService.instance`
  - Replace direct HTTP calls with `ApiService.instance.get/post`
  - Update tabs that receive CameraController as param to use CameraService
  - Run: `cd C:/Users/MiEXCITE/Projects/gtd3d/companion_app && rtk flutter.bat build apk --debug`
  - **COMMIT**: `rtk git add -f companion_app/ && rtk git commit -m "feat(apk): implement 3 services (API, camera, sensor) + wire into HomeScreen"`

---

## Wave 3C — Web Studio Live Mode (3 tasks)

Goal: Make the web studio work with a real backend (MOCK_MODE=false) and add a toggle.

### Prerequisites
- Independent of Waves 3A/3B (can run in parallel via worktree)
- Studio files: `apps/web_app/static/studio/`
- Backend: py4web at `:8000`, studio HTML at `/web_app/studio_v2`

### Tasks

- [x] **3C.1 — Add MOCK_MODE toggle UI**
  - In `studio.js`: keep `MOCK_MODE: true` as default (safe for devs without backend)
  - Add a toggle button in the studio top bar. When clicked, flip `Studio.MOCK_MODE` and reload panels.
  - CSS: small toggle switch or button in top-right, label "Mock" / "Live"
  - **Don't**: Change any panel logic yet. Just the toggle mechanism.

- [x] **3C.2 — Harden panels for MOCK_MODE=false**
  - Grep `MOCK_MODE` across all `studio/*.js` files to see which panels check it
  - For panels that DON'T check: add graceful fallback when `Studio.apiCall` fails (show "Backend unavailable" message, don't crash)
  - Panels to check: `scan-panel.js`, `render-panel.js`, `device-panel.js`, `progress-panel.js`, `texture-panel.js`, `customer-panel.js`, `body-scan-panel.js`, `report-panel.js`
  - Mock-only panels (`3dgs-panel.js`, `lhm-panel.js`, `multi-capture-panel.js`): add a banner "Requires backend — enable Live mode and start py4web"
  - **Don't**: Implement actual API calls for mock-only panels. Just add the error state.

- [x] **3C.3 — Wire viewport + commit**
  - In `render-panel.js`: find "View 3D" button click handler. On click, call `Studio.showInViewport('glb', url)`.
  - In `studio.js`: add `showInViewport(type, url)` method that dispatches `CustomEvent('viewport-load', {detail: {type, url}})`.
  - In `viewport.js`: listen for `viewport-load` event, load the GLB/mesh into the Three.js scene.
  - **COMMIT**: `rtk git add -f apps/ && rtk git commit -m "feat(studio): live mode toggle + viewport wiring + panel error states"`

---

## Agent Dispatch Plan

```
Wave 3A (APK screens)  ──→ Wave 3B (APK services)  [sequential, B depends on A]
Wave 3C (Web studio)   ──→ [independent, parallel worktree]
```

Recommended dispatch:
- Agent 1: Wave 3A then 3B (same worktree, sequential)
- Agent 2: Wave 3C (separate worktree, parallel)
- Merge both, then verify full build
